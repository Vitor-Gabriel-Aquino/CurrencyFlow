[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'php artisan passport:keys --force && php artisan config:clear && php -S 0.0.0.0:${PORT:-8000} -t public public/index.php'"
